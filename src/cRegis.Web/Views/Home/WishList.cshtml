@model cRegis.Web.ViewModels.WishListViewModel
@{
    ViewData["Title"] = "Wishlist";
}
<h1>WishList</h1>

<div class="container">

    @foreach (var courseVM in Model.courses)
    {
        @await Html.PartialAsync("_courseContainer", courseVM);
    }

</div>