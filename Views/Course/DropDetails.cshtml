@model cReg_WebApp.Models.ViewModels.CourseViewModel;

@{
    ViewData["Title"] = "RegisterDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Name"] = @Model.user.name;
    ViewData["studentId"] = @Model.user.studentId;
}

<h1>Details</h1>
<div>
    <h4>Course</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.thisCourse.courseName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.thisCourse.courseName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.thisCourse.courseId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.thisCourse.courseId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.thisCourse.courseDescription)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.thisCourse.courseDescription)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.rate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.rate)
        </dd>
    </dl>
</div>
<table class="table">
    <tr>
        <th>
            Comments
        </th>
        <th>
            @Html.DisplayFor(modelItem => Model.commentNum)
        </th>
        <th></th>
    </tr>

    @foreach (var keyPair in Model.keyParis)
    {
        <tr>
            <th>
                @Html.DisplayFor(modelItem => keyPair.Key)
            </th>
            <td>
                @Html.DisplayFor(modelItem => keyPair.Value)
            </td>
        </tr>
    }

</table>
<div>
    <h4><a asp-controller="Course" asp-action="Drop" asp-route-sid="@Model.user.studentId" asp-route-eid="@Model.enrollId">Drop</a></h4>
</div>




